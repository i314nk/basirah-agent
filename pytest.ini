[pytest]
markers =
    integration: Real-world integration tests that make actual API calls
    slow: Slow tests that take more than 30 seconds
    expensive: Expensive tests that cost money to run (API calls)
